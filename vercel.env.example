# Vercel Environment Variables (Production)
# Copy these into Vercel → Project Settings → Environment Variables
#
# Production backend (this project): https://glovia-backend.vercel.app
# API base URL for frontend:        https://glovia-backend.vercel.app/api/v1

NODE_ENV=production
PORT=3001
API_PREFIX=api/v1

DATABASE_URL=mongodb+srv://glovianepal_db_user:user@glovia.trmllne.mongodb.net/glovia_nepal?retryWrites=true&w=majority

REDIS_HOST=HOST
REDIS_PORT=6379
REDIS_PASSWORD=

JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this
JWT_REFRESH_EXPIRES_IN=30d

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@glovia.com.np
SMTP_PASSWORD=your-sendgrid-api-key-or-app-password
SMTP_FROM_NAME=Glovia Nepal
SMTP_FROM_EMAIL=noreply@glovia.com.np

# Comma-separated: your production frontend (and www) – used for CORS
FRONTEND_URL=https://glovia.com.np,https://www.glovia.com.np

THROTTLE_TTL=60
THROTTLE_LIMIT=10

MAX_FILE_SIZE=5242880
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/webp

BASE_DELIVERY_CHARGE=100
FREE_DELIVERY_THRESHOLD=2000
VALLEY_DELIVERY_CHARGE=100
OUTSIDE_VALLEY_CHARGE=150

DEFAULT_ADMIN_EMAIL=admin@glovia.local
DEFAULT_ADMIN_PASSWORD=AdminPass123!

SMS_GATEWAY=mock

SPARROW_SMS_TOKEN=your_sparrow_token_here
SPARROW_SMS_FROM=GloviaNepal

NTC_SMS_USERNAME=your_ntc_username
NTC_SMS_PASSWORD=your_ntc_password
